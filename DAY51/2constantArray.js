// constant array
// in the constant array we can use array function but we cannot change complete the array
// arr=[1,2,3] like this we cannot do
const arr=[1,2,3];

arr.push(4);
console.log(arr);
arr.pop();
console.log(arr);
